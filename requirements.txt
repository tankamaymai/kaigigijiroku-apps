# 議事録自動生成アプリの依存関係
#
# インストール方法:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# ffmpegも必要です:
#   brew install ffmpeg (Mac)

# Excel操作
openpyxl>=3.1.0

# Word操作
python-docx>=1.0.0

# Whisper (高速版 + フォールバック用CLI版)
faster-whisper>=1.0.0
openai-whisper>=20231117

# OpenAI API (ChatGPT)
openai>=1.0.0

# Google Gemini API
google-genai>=1.0.0

# HTTP クライアント (Ollama通信用)
httpx>=0.27.0

# Webサーバー
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# アプリ化 (オプション)
pyinstaller>=6.0.0
